# Get a key from https://console.typesafe.ai/settings/keys
# Without this the server falls back to the built-in Jev simulator and says so in the HUD.
TYPESAFE_API_KEY=

# Optional overrides
# TYPESAFE_MODEL=jev-latest
# PORT=8787
